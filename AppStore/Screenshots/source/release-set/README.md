# CatLocal 1.0 Screenshot Sources

These sources render at the accepted iPhone `1320 x 2868` portrait size.

- `02-camera-*` uses genuine localized iOS 26 Release captures of the
  capture/import start state.
- `03-cutout-*` uses a genuine physical-iPhone CatLocal cutout capture and the
  genuine cutout produced from the selected hero photograph. Photograph-rights
  verification remains deferred and is not represented as complete.
- `04-privacy-*` uses genuine localized iOS 26 Privacy Receipt captures.
- The lead English hero remains
  `../hero-en/device.html`.
- The lead Turkish hero is `../hero-en/device-tr.html` and uses the same
  genuine physical capture beneath localized marketing copy.

The Turkish cutout composition currently uses the genuine English physical
capture beneath a Turkish marketing headline. Replace that inner capture after
the deferred native Turkish review if a fully Turkish product-page set is
required. No UI text inside the physical capture has been redrawn.

Final exports must be RGB, `1320 x 2868`, and contain no alpha channel.
